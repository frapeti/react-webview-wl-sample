# Product Overview — react-webview-wl-sample

> Keep this document synchronized with the code. Update it whenever the product's purpose, customers, or ecosystem relationships change.

## What it is
React Native WebView sample for white label web integration

## Business value & purpose
React Native WebView sample for white label web integration. Detailed behavior is described in [architecture.md](./architecture.md).

## Who consumes the output
Not documented in the repository. Check README, code comments, and the org inventory in `ualabee-software-orchestrator` for clues.

## Ecosystem relationships
- Role in the Ualabee ecosystem: see the GitHub org inventory (`ualabee-software-orchestrator` → `specs/ecosystem/github-org-inventory.md`).
- Related repositories in the org inventory: not listed.

## External constants to preserve
No `.env.example` found. Preserve any DB names, hosts, project IDs, city IDs, API URLs, and bucket names exactly as they appear in code and configuration.
